on:
  push:
    branches:
      - master 



jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3     
      - name: "login to docker hub"
        run: docker login --username tasneemkhaled350 --password dckr_pat_sO_SIW-3J3hlS4ObG_GPYeM10gU
      
